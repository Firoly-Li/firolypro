spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/firolypro?characterEncoding=utf-8&useSSL=false

  jpa:
    show-sql: true   #控制台显示建表信息
    hibernate:
      ddl-auto: update  #update，没有表就新建表，有表更新操作